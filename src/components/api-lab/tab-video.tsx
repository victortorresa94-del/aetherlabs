'use client';

import React, { useState } from 'react';

const TabVideo = () => {
    return (
        <div className="flex items-center justify-center h-[400px] text-zinc-500 font-mono">
            <div className="text-center">
                <span className="text-4xl block mb-4">🚧</span>
                <p>Video Generation (Veo) is currently in private preview.</p>
                <p className="text-xs mt-2 text-zinc-600">This tab is a placeholder for future implementation.</p>
            </div>
        </div>
    );
};

export default TabVideo;
